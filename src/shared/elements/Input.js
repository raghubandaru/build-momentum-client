import styled from 'styled-components'

export const Input = styled.input`
  padding: 1rem 2rem;
  background: #d9e2ec;
  color: inherit;
  font-size: 1.5rem;
  font-family: inherit;
  border: none;
  line-height: inherit;
`
